using UnityEngine;
using System.Collections;

public class Objectives : MonoBehaviour 
{
	/*
	public float ChangeTextTwo = 8f;
	public float ChangeTextOne = 4f;
	
	float restartTimer;
	
	// Update is called once per frame
	void Update()
	{
		
		GetComponent<Text>().text = "Kill all the Zombies!";
		
		restartTimer += Time.deltaTime;

		if( restartTimer >= ChangeTextTwo )
		{
			GetComponent<Text>().text = "";
		}

	}*/
}
